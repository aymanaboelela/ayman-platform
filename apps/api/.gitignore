# Prisma 7 generated client — regenerated by `prisma generate`, never committed.
src/generated/

# Local media upload storage (Plan 6 Task 13). Never committed — this is
# exactly why Global Constraint 10 forbids `git add -A` in this plan.
.media/
