# منصة م. أيمن أبو العيلة

منصة تعليمية عربية لطلاب **البرمجة وعلوم الحاسب** في نظام **البكالوريا المصرية**.

> **الحالة:** الأساس (Plan 1) خلص. الأوث والمحتوى والكويزات لسه جايين في الخطط 2–7.

---

## التشغيل محليًا

**المتطلبات:** Node 24+، pnpm 11، PostgreSQL 16.

```bash
pnpm install

# إنشاء الداتابيز والأدوار الثلاثة (مرة واحدة بس)
psql -d postgres -f scripts/db-bootstrap.sql

# انسخ ملف البيئة وعدّل الباسوردات لو غيّرتها في السكريبت
cp .env.example apps/api/.env

pnpm --filter @ayman/api exec prisma migrate deploy
pnpm --filter @ayman/api exec prisma generate
pnpm db:seed

pnpm dev
```

| الخدمة | البورت |
|---|---|
| الويب (Next.js) | `3200` |
| الـ API (NestJS) | `3300` |

بعدها افتح `/dev/tokens` عشان تشوف نظام التصميم، و`/dev/taxonomy` عشان تشوف بيانات البكالوريا جاية من الداتابيز.

---

## البنية

```
apps/web            Next.js 16 · App Router · Tailwind 4 · عربي RTL
apps/api            NestJS 11 · Prisma 7 · هو الوحيد اللي بيكتب في Postgres
packages/contracts  Zod 4 — سكيما واحدة يتشاركها الفرونت والباك + كل النصوص العربية
packages/ui         design tokens + مكوّنات
packages/config     eslint / tsconfig presets
```

### قرارات معمارية أساسية

**أصل واحد (Single origin).** الويب على `/` والـ API على `/api` ورا نفس الأصل. ده قرار أمني مش راحة — بيخلي `__Host-` cookies و`SameSite=Strict` ممكنين، و**مفيش CORS خالص** في أي مكان.

**صلاحيات أقل ما يمكن في الداتابيز.** تلات أدوار: `ayman_owner` (الميجريشن بس)، `ayman_runtime` (قراءة وكتابة بس، **مفيش DDL**)، `ayman_readonly`. يعني لو حصل SQL injection مش هيقدر يعمل `CREATE FUNCTION` ولا `DROP TABLE`.

**RTL أصلي مش مقلوب.** في ESLint rule بترفض `ml-*` و`left-*` وأخواتهم، وبتشوف جوه `cn()` و`clsx()` والـ ternaries.

**كل النصوص في مكان واحد.** مفيش أي نص عربي مكتوب جوه المكوّنات — كله في `packages/contracts`. عشان إضافة الإنجليزي بعدين تبقى تغيير في الراوتنج مش إعادة كتابة.

---

## الأوامر

```bash
pnpm dev         # الاتنين مع بعض
pnpm lint        # كل الحزم
pnpm typecheck
pnpm test
pnpm db:seed     # 27 محافظة + النظامين + المسارات + المواد
```

---

## نظام البكالوريا

| النظام | المجموع | النجاح | التحسين |
|---|---|---|---|
| البكالوريا المصرية | 600 | 70% | لحد 4 مرات |
| الثانوية العامة | 320 | 50% | لأ |

النظامين **بيشتغلوا مع بعض**، البكالوريا مش بديل. أول امتحان بكالوريا **يونيو 2027** — يعني مفيش طلبة صف تالت بكالوريا لحد 2027/2028، والصف التاني لوحده **67%** من المجموع.

المسارات الأربعة: الطب وعلوم الحياة · الهندسة وعلوم الحاسب · الأعمال · الآداب والفنون.

---

## التوثيق

- [التصميم والمواصفات](docs/superpowers/specs/2026-07-25-ayman-platform-design.md)
- [تقرير البحث](docs/research/2026-07-25-research-brief.md) — دراسة للمنصات المصرية ومحركات الامتحانات
- [خطة التنفيذ](docs/superpowers/plans/2026-07-25-plan-1-foundation.md)

---

## ملاحظة أمنية

`scripts/db-bootstrap.sql` فيه باسوردات تطوير محلية بشكل مقصود. **متستخدمهاش على سيرفر حقيقي** — غيّرها من متغيرات البيئة عند النشر.
